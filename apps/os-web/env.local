# public basepath, if given, is a project relative base path that overwrites everything and uses this as basepath for exploring files. might later be useful for creating explorable knowledgebases that are deployed on a custom domain, automatically.
# NEXT_PUBLIC_BASEPATH=
NEXT_PUBLIC_TITLE=Clarity AI