Beware: the following paths should never be used in the projectroot as folder names, because of the current pages structure, they could corrupt!

`database/*`
`deposit/*`
`function/*`
`landing/*`
`operations/*`

For the rest you can use anything.
